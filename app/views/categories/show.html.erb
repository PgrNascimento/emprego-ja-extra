<div class="row">
  <h1><%=@category.name%></h1>
</div>

<div class="row">
  <div class="jobs">
    <%if @category.jobs.empty? %>
      <div class="alert alert-warning" role="alert">Nenhuma vaga disponível</div>
    <%end%>
    <%@category.jobs.each do |job| %>
      <%= render job %>
    <%end%>
  </div>
</div>

<%= link_to "Voltar", root_path %>
